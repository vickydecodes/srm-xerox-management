import { View, Delete, ActiveStatus, Retrieve, Erase } from "./bill.modal.components";

export const modals = {
  view: View,
  delete: Delete,
  activeStatus: ActiveStatus,
  retrieve: Retrieve,
  erase: Erase
};