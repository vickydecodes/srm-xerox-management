import { View, Create, Edit, Delete, ActiveStatus, Retrieve, Erase, ResetPassword } from "./shop-admin.modal.component";

export const modals = {
  view: View,
  create: Create,
  edit: Edit,
  delete: Delete,
  activeStatus: ActiveStatus,
  retrieve: Retrieve,
  erase: Erase,
  resetPassword: ResetPassword
}