import { Create, Edit, Delete,Retrieve,Erase,ActiveStatus,View } from "./service.modal.components";

export const modals = {
  create: Create,
  edit: Edit,
  delete: Delete,
  activeStatus: ActiveStatus,
  retrieve: Retrieve,
  erase: Erase,
  view:View,
};
