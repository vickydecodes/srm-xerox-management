import { ActiveStatus, Create, Delete, Edit, Erase, Retrieve } from "./inventory-product.modal.components";

export const modals = {
  create: Create,
  edit: Edit,
  delete: Delete,
  erase: Erase,
  retrieve: Retrieve,
  activeStatus: ActiveStatus,
};