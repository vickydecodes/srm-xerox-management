import {
  Create,
  Delete,
  Erase,
  Retrieve,
  ActiveStatus,
  View,
} from "./shop.modal.component";

export const modals = {
  create: Create,
  delete: Delete,
  erase: Erase,
  retrieve: Retrieve,
  activeStatus: ActiveStatus,
  view: View,
};