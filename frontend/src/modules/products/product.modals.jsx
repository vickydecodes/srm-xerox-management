import { Create, Edit, Delete, Erase, Retrieve, ActiveStatus,View } from "./product.modal.components";

export const modals = {
  create: Create,
  edit: Edit,
  delete: Delete,
  erase: Erase,
  retrieve: Retrieve,
  activeStatus: ActiveStatus,
  view: View,
};